touch .gitignore
git config --global user.name Gargie
git config --global user.email "garginimbalkar10@gmail.com"
git init
git add .
git commit -m "Initial Commit"
git status # Just to see
$ ssh-keygen -t rsa -b 4096 -C "garginimbalkar10@gmail.com"
tail <path to is_rsa.pub> # add this key to github account
git remote add origin <ssh url.git>
git push origin master
